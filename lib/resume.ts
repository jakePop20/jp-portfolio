/**
 * Public resume PDF. URL-safe path (no spaces) so links work reliably on Vercel
 * and other Linux-based hosts. File: `public/jakub-popiolek-resume.pdf`.
 */
export const RESUME_PDF_PATH = "/jakub-popiolek-resume.pdf" as const;

export const RESUME_DOWNLOAD_FILENAME = "Jakub-Popiolek-Resume.pdf" as const;
